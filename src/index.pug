extends layout/main.pug

block content
    .content
        select
            option(disabled selected) Cортировать: 
            option(value='ABC') По алфавиту
            option(value='CBA') В обратном порядке
        .content-main
